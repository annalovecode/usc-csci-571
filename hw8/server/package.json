{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "serve": "node dist/index.js",
    "watch": "tsc-watch --noClear --onSuccess 'npm run serve'",
    "start": "npm run build && npm run serve",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@types/express": "^4.17.8",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "tsc-watch": "^4.2.9",
    "tslint": "^6.1.3",
    "typescript": "^4.0.3"
  }
}
